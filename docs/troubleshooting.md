# Pipeline Troubleshooting Ledger

Read this file before running generation or live commands. Add an entry only
after the root cause and workaround are verified. Sanitize commands and output;
never include `.env` values, API logins, tokens, private payloads, or raw
captures.

| Date | Sanitized command/context | Symptom | Root cause | Safe workaround | Prevention | Verification |
|---|---|---|---|---|---|---|
| 2026-07-16 | `uv run pytest -q` in a fresh checkout, without live arguments | `pytest` executable could not be spawned | The generated project declared Poetry dev dependencies, but no PEP 735 `dependency-groups.dev` for `uv sync` | Use an isolated `uvx` pytest only to observe TDD RED, then add and sync the explicit dev group | Keep test tooling in `[dependency-groups].dev` and use `uv sync --frozen --group dev` in CI | `uv sync --group dev` installed pytest and the focused Task 1 suite passed |
| 2026-07-16 | `uv sync --group dev` after adding pytest 9 | Resolver rejected the Python 3.9 split although the active interpreter was 3.12 | `uv` resolves every version admitted by `project.requires-python`; pytest 9 requires Python 3.10+ while the legacy metadata admitted 3.9 | Raise `project.requires-python` to the already selected SDK minimum, Python 3.10 | Keep runtime support metadata aligned with generator and development-tool requirements | Resolution completed with 53 packages and the focused Task 1 suite passed |
| 2026-07-16 | Cached `sdd-workspace` and `task-brief` helper scripts invoked directly | `Permission denied` before helper startup | The cached scripts had mode `0644` despite bash shebangs, so direct execution lacked execute permission | Invoke each helper as `bash SCRIPT ...`; after `bash sdd-workspace ...`, invoke `bash task-brief ... OUTPUT_PATH` explicitly because the workspace helper's nested direct call has the same mode assumption | Avoid direct execution of cached SDD helpers and always pass an explicit task-brief output path | `stat` confirmed mode `0644`; explicit `bash` invocations created the workspace metadata and task brief |
| 2026-07-17 | A hand-written Overlay guard used a copied SHA-256 constant for the JSON value `"bool"` | The nominal fixture update failed closed as stale because its recorded digest differed from the pipeline digest | `canonical_json_bytes` appends a trailing newline, but the copied constant was not produced by that helper | Compute every guard digest with `sha256_bytes(canonical_json_bytes(value))` from the exact matched JSON value | Never hand-type or copy an unverified overlay guard hash; assert fixture hashes against the canonical helpers in offline tests | The corrected fixture digest is asserted by `test_guarded_primitive_update_does_not_mutate_source`, and the focused Overlay suite passes |
| 2026-07-17 | `uv run ruff check .` before the legacy generated SDK is replaced | Ruff reported thousands of pre-existing findings under root-level `iikocloud_client/` and `test/` | The current Ruff exclusions name the future `src/iikocloud_client` layout, while the checked-in legacy generator output still lives at the repository root | During pipeline tasks, lint the changed pipeline module and its hand-written test directly; keep the full-tree result visible until the publication task replaces the legacy output | Update full-tree lint scope together with the generated-package layout instead of bulk-editing legacy generated files | `uv run ruff check tools/openapi_pipeline/generator.py tests/pipeline/test_generator.py` passes, while the ordinary offline pytest suite remains green |
| 2026-07-17 | Public `bootstrap` after the upstream candidate was saved | Candidate generation stopped at `Cannot deterministically resolve operationId collision` for two POST operations with the same request-derived verb phrase | The naming fallback added only the HTTP method, which cannot distinguish different routes that share both a request schema and method | Do not fetch again; reuse the saved `build/upstream/candidate.json` through the injectable local preview stage, fall back duplicate request phrases to normalized paths, and use the HTTP method only for remaining path collisions | Keep a synthetic same-request/same-method/different-path regression and fingerprint the raw candidate before and after every local replay | The regression failed on the original collision and passed after the tiered fallback; the offline replay produced all bootstrap candidates and the raw SHA-256 stayed unchanged |
| 2026-07-17 | Targeted read-only inspection of the saved public OpenAPI candidate with `jq` | The shell reported `jq: command not found` for each attempted query | The host checkout does not provide the optional `jq` executable | Use `uv run --frozen --offline python -c` with the standard-library JSON parser for narrowly scoped local reads | Do not assume optional system JSON tools are installed; prefer the pinned project interpreter for candidate inspection | The offline project-Python query completed and returned only the requested public schema nodes without another fetch |
| 2026-07-20 | `uv run --frozen --offline mypy tools/openapi_pipeline` before the legacy generated SDK is replaced | Mypy reported 27 existing errors in 14 root-level generated SDK files | Recursive pipeline checking includes `tools/openapi_pipeline/live/generated.py`; mypy follows that module's imports into the current legacy `iikocloud_client`, which still contains unresolved generated aliases/type defects and lacks a visible `dateutil` stub | For an isolated pipeline slice, pass the exact changed pipeline and test files to mypy; do not suppress import following globally | Expand the mypy target only when the Task 11 generated-package replacement and dependency metadata are complete | `mypy tools/openapi_pipeline/live/generated.py` reproduced the same 27 imported-package errors, while the exact four D1 source/test targets completed with no issues |
| 2026-07-20 | Filesystem-safety test creating an `AF_UNIX` socket below pytest's nested `tmp_path` | Socket bind failed repeatedly with `AF_UNIX path too long` before the writer was exercised | Linux pathname Unix sockets have a short fixed address limit, while the pytest node name made the absolute temporary path exceed it | Temporarily enter the test-owned candidate directory, bind only a short relative socket name, and restore the original working directory in `finally` | Keep adversarial socket entry names short and never bind them through the full pytest temporary path | The socket special-file regression completed and the focused evidence candidate writer suite passed |
| 2026-07-20 | Evidence candidate persistence fails after allocating a hidden staging directory | A mode-safe `.evidence-candidates.tmp-*` residue, or a complete final tree after a post-rename failure, remains under the ignored build root | Name-based automatic cleanup cannot prove that a same-user concurrent process has not replaced an entry between validation and deletion | Inspect the ignored residue locally and resolve it explicitly before rerunning; the writer intentionally closes descriptors but never deletes or repairs names. A complete final tree with no hidden residue is recoverable as an exact no-op | The writer enforces a persistent uid-owned mode-0644 `.evidence-candidates.lock` for all cooperating official writers and, while holding it, blocks on every exact `.evidence-candidates.tmp-*` prefix before candidate validation or staging. A non-cooperating same-UID process remains out of scope because it can directly mutate the checkout | Lock creation, contention, binding, lifetime, residue-kind, failure, collision, umask, rebind, recovery, and syscall-boundary regressions pass while monkeypatched `unlink` and `rmdir` remain uncalled |
| 2026-07-20 | Offline tests perform many deeply composed evidence/schema validations in one long-lived pytest process | CPython can terminate with a segmentation fault instead of raising a managed recursion error; the final frame is order- and native-stack-headroom-dependent and can surface in a later evidence/store validation after both saved-public smokes were deselected | Repeated recursive evidence/schema traversal can exhaust the native stack before a later call; faulthandler showed the failing path re-entering `_strict_document_copy` through analysis and bundle construction, while the exact reported store node and the same tests in fresh small processes were stable | Use a deterministic fresh-process split: writer alone; candidate contract/store/builder without `locally_composed`; analyzer/promotion reader without `locally_composed`; each saved-public smoke as its own exact node; capture, live-support, and remaining pipeline tests as separate groups. Reuse saved public inputs and make no network request | Replace or strictly bound the recursive schema walk in its own reviewed task; until then keep process isolation as an explicit verification gate and never hide the failed long-lived run | The long-lived run exited 139 in a store node; that exact parametrized node then passed 3 tests fresh, and the isolated groups passed 99, 65, 141, 1, 1, 103, 210, and 270 tests respectively, with one unrelated policy deselection |
| 2026-07-20 | Guarded read-target discovery stopped immediately after an HTTP 200 authentication response | The strict parser and its synthetic fixtures expected only `token`, while the saved OpenAPI contract requires the exact response fields `correlationId` and `token` | Do not retry blindly; preserve the recorded rate reservation, update the parser to validate the exact documented object, validate and discard the correlation UUID, and let the guard admit any later run | Build auth fixtures from the response schema's required fields and keep missing, extra, duplicate, invalid-UUID, and invalid-token regressions | Focused session, capture, and discovery tests passed, then one guarded discovery run completed successfully without a 429 |
| 2026-07-20 | Offline promotion of sanitized external-menu V2/V3/V4 evidence against the latest public schema | Validation stopped one mismatch at a time on false arrays, lost nullability, UUID-keyed maps, a missing public property, null-only undeclared barcode fields, and item-type enum drift; second-pass sanitization also changed its own safe tokens, while captures made before the enum repair contained an ambiguous string marker | The upstream schema had regressed several established JSON contracts, the validator reported only the first mismatch, fixed-point mode did not preserve its exact sentinel set, and the original redaction hints omitted the historical public `SERVICE` literal | Maintain one immutable exact-fragment repair registry that produces both a detached validation view and guarded overlay actions; bind dynamic-map exceptions to exact schema paths, preserve only the four exact safe tokens in fixed-point mode, reject ambiguous enum markers, and recapture each affected version with corrected schema-aware hints at the guarded cadence | Aggregate safe-path mismatch audits must precede fixes; full builders require every repair target; unknown fragments, paths, literals, non-null opaque fields, and overlay reapplication fail closed | Focused offline suites passed in fresh processes; guarded schema-aware V2/V3/V4 captures completed at the 30-second floor without `429`; offline promotion and explicit acceptance produced exactly five reviewed payloads |
| 2026-07-20 | Pinned OpenAPI Generator validation of the reviewed effective schema | Validation reported ten component schema names that violate the generator name regex | New upstream CLR-generic component keys had reviewed output names in `model-name-overrides.yaml`, but the registry was applied only to generator config after `validate -i effective.json` had already inspected the physical keys | Before strict validation, use the same explicit reviewed registry to rename only generator-invalid component keys and rewrite exact local schema references; fail on missing overrides, unsafe targets, collisions, encoded non-local refs, or remaining old refs | Keep physical normalization as one pure naming stage shared by bootstrap, committed sync, and reviewed-candidate composition; never skip generator validation | Current public composition renamed ten keys and eleven references with no stale names; pinned validation with container networking disabled and the full atomic bootstrap both completed successfully |
| 2026-07-20 | Isolated generated-wheel import smoke after successful code generation | All generated modules imported, then the provenance check raised `FileNotFoundError` while resolving an absent virtual-environment site root | `site.getsitepackages()` reported one existing wheel root plus distro-layout aliases below a missing `venv/local`, while the smoke script resolved every reported path with `strict=True` in one list comprehension | Resolve each reported site root strictly, ignore only `FileNotFoundError` for a missing reported root, and retain the requirement that the imported package is contained by an existing resolved site-packages root | Keep real isolated-venv coverage with both an existing and guaranteed-missing reported root, plus the broken-wheel and checkout-escape regressions | The exact previously failing isolated smoke completed with exit 0, all package-check tests passed, and the full atomic bootstrap completed successfully |
| 2026-07-20 | Strict offline `verify` after importing or testing the generated package from the checkout | Final generated-tree comparison reported hundreds of extra ignored `__pycache__/*.pyc` files while no manifest file was missing | CPython wrote bytecode inside the manifest-controlled `src/iikocloud_client` tree, and exact verification correctly treated those runtime files as unreviewed additions | Run atomic offline `sync` to restore a clean generated tree, then use `PYTHONDONTWRITEBYTECODE=1` for ad hoc checkout imports and the immediate verification; never add bytecode to the manifest | The root pytest configuration disables bytecode before project imports, and live setup checks the exact `src` package origin after artifact preflight but before private profile access, then loads generated runtime classes lazily | A clean offline sync followed by verification completed with exit 0; focused pytest, provenance, Ruff, and mypy checks passed and left zero bytecode files in the generated `src` tree |
| 2026-07-20 | A generated async read smoke completed its call, then failed while closing the session-scoped generated client | Fixture teardown raised `RuntimeError: Event loop is closed` after the test call had passed | The session-scoped async fixtures used pytest-asyncio's session runner, while the async test used the default function runner; the HTTP transport bound lazily during the call to the function loop, which closed before session teardown | Mark each async live test with `pytest.mark.asyncio(loop_scope="session")` and declare its session-scoped async client fixtures with `pytest_asyncio.fixture(scope="session", loop_scope="session")` | Any test that uses a session-lived async client must explicitly share the session loop; keep a synthetic loop-bound client regression that exercises setup, call, and teardown without network access | The regression first reproduced the teardown failure exactly, then passed with the explicit loop scopes; all 238 offline live-support tests plus focused Ruff and mypy checks passed |
| 2026-07-21 | Offline private JSON report update with a synthetic concurrent leaf replacement | A replacement inserted after validation but before atomic publication was silently overwritten | Check-then-unconditional atomic replace does not compare the destination inode at the publication boundary | Stage the new private file descriptor-relatively, atomically exchange stage and target with `renameat2(RENAME_EXCHANGE)`, inspect the swapped-out leaf against the expected bytes and metadata, and exchange back on mismatch before removing only a revalidated writer-owned stage | Use compare/exchange plus verified rollback whenever publication must both detect concurrent replacement and preserve an unrelated destination; fail closed and retain any uncertain artifact | The pre-publication replacement regression failed against unconditional replacement, then passed with the unrelated marker restored; the focused 11-test review group and 84-test Task 7 group passed offline |
| 2026-07-22 | Offline synthetic safety-boundary tests for normalized pipeline errors | A fixed `SafetyError` raised with `from None` still retained the caught exception in `__context__` | Python assigns `__context__` whenever a new exception is raised while an `except` handler is active; `from None` suppresses display but does not detach the object | Record failure in a boolean or result inside `except`, leave the handler, then raise the fixed error | Keep regressions asserting `__context__ is None` and no source exception or payload rendering at sanitized boundaries | Five focused context regressions failed on attached exceptions, then passed with all 26 read-runner tests after the exhaustive mechanical audit |
| 2026-07-21 | Offline reconciliation of the stop-list read-case plan with the generated SDK | The planned `CheckStopListItem` symbol did not exist even though `CheckStopListRequest.items` was present | The effective schema references the discriminated `OrderItem` base; the generator emits `DeliveryOrderCreateItem` plus the concrete `DeliveryOrderCreateProductItem` mapping for `type=Product`, not a stop-list-specific item class | Construct exactly one generated `DeliveryOrderCreateProductItem` with `type="Product"` and pass that model instance through `CheckStopListRequest.items` | Name polymorphic request items from the reviewed generated mapping, and keep an offline serialization regression that checks the concrete subtype and its exact JSON fields | The effective-schema discriminator mapping matched the generated classes; an offline model round trip retained the concrete subtype and serialized exactly `amount`, `price`, `productId`, and `type` |
| 2026-07-21 | Offline reconciliation of planned bounded delivery search with generated request fields and approved context seeds | The plan called the search ordering stable and described a 48-hour window, but the generated sort enum has no stable tie-break field and the approved fixture supplies its own exact bounded endpoints | `OrdersByDeliveryDateAndFilterRequest` exposes only business-field sorting, while duration is owned by the previously reviewed hidden seed rather than the delivery case | Reuse `window_from_local` and `window_to_local` exactly; request `CompleteBefore` with `Ascending` and `rowsCount=1`, explicitly treating equal timestamps as server-ordered | Derive query-bound claims from both the generated model and the actual seed producer; assert exact aliases, enum members, omitted free-text/status fields, and unchanged seed endpoints offline | Generated request inspection confirmed the available sort enum and absence of a tie-breaker; `tests/conftest.py` confirmed the exact seeded endpoints |
| 2026-07-21 | Offline reconciliation of the planned courier-role read with generated employee models | The plan expected a response-derived role UUID, but `get_couriers_by_role` accepts short role-code strings and `get_couriers` exposes no role field | `CouriersAndCheckRoleRequest.rolesToCheck` is `List[str]`; `EmployeeDirectoryEntry.code` is the employee code and must not be reinterpreted as a role | Name the conditional context value `employee_role_code`, never derive it from courier PII or employee code, and return `employee_role_unavailable` before rate acquisition until a separate reviewed role-code provider exists | Verify provider and consumer semantics against both generated request and response fields before connecting context keys; keep an offline no-role regression | Generated model inspection confirmed the string request field, absence of any courier role field, and the distinct employee-code description |
| 2026-07-21 | Offline reconciliation of planned loyalty/customer reads with generated discriminator, coupon, report, and order models | The plan treated a strict customer ID string as a UUID, applied date/page fields to the revision report, implied coupons were nested in the series response, and omitted price/size requirements from the check-in item | The four endpoints use distinct generated shapes: the ID discriminator subclass stores `id` as `StrictStr`; revision reports expose only `pageSize` plus optional revision/transaction ID; series and coupon numbers arrive from separate responses; a product item requires price and any matching size | Convert only the response-derived customer UUID to `str`; give the date and revision reports their own exact fields; chain series number into a one-row non-activated request before extracting coupon number; recover the selected product's exact price/size tuple from immutable nomenclature context | Audit every nested request and response model, not only the top-level Appendix binding, and serialize exact synthetic requests offline | Generated field inspection confirmed all aliases/requiredness, the discriminator mapping, separate coupon paths, report shapes, and concrete product-item requirements |
| 2026-07-21 | Offline reconciliation of finance read cases with generated request and response models | Passing the shared organization UUID directly fails strict request validation, while a generic transaction/list design could add unsupported dates or assume wrapper/store fields | All finance request GUIDs are `StrictStr`; only account transactions expose `from`/`to`; service lists and document transactions return bare lists; finance service list items expose a revenue account but no store or expense account | Convert the organization UUID to its canonical string in every finance builder, construct each request from only its generated fields, validate bare-list element types, and extract document/account targets from the same confirmed non-deleted item | Inspect concrete generated fields and return annotations before connecting generic factories; preserve offline serialization, exact-type, same-item, and no-target-before-invocation regressions | Generated model inspection confirmed strict GUID strings, the `from` alias, the absence of document-transaction dates and finance service stores, and each endpoint's exact response shape |
| 2026-07-21 | Focused mypy validation of a namespace-package test and the implementation it imports | Passing both filesystem paths in one mypy command reported the implementation twice under `finance` and `tests.integration.read.cases.finance` | The test tree intentionally has no `__init__.py`, and direct path discovery conflicts with the fully qualified module reached through the test import | Run mypy on the focused test entrypoint only; it follows the implementation through the canonical import and still checks both files | For namespace-package tests, choose one import root per mypy invocation instead of listing an imported module and its importer together | The single-entrypoint command removed the duplicate-module error and exposed the real annotations to fix |
| 2026-07-21 | Offline reconciliation of inventory list/get, counteragent, and cost-price reads with generated models | A uniform document assumption would reject invoice records, pass UUID objects into strict-string requests, send a date-only cost timestamp, and validate two wrapper responses as lists | Eight list families expose nullable `deleted`, incoming/outgoing invoices expose only `status`; every inventory request GUID is `StrictStr`; `dateIncoming` is documented as a full offset timestamp; counteragents and cost prices return exact wrapper models | Override invoice activity with `NEW`/`PROCESSED`, convert UUID seeds to canonical strings, expand the UTC date seed to midnight with milliseconds and `+00:00`, and validate each endpoint's concrete response shape | Audit concrete list and get return annotations plus nested request models before applying a shared factory; test exact aliases, activity semantics, same-item extraction, fixed store priority, and wrapper-vs-list rejection offline | Generated code inspection confirmed all ten list/get model pairs, the mixed activity fields, string GUIDs, nested `PriceItem`, full timestamp description, and both wrapper responses |
| 2026-07-21 | Offline assembly of the finance-to-inventory context dependency graph | Five inventory providers used mechanically shortened keys such as `inventory_incoming_invoice_id`, so finance consumers could not find their declared `inventory_incoming_invoice_document_id` providers | Endpoint family names and semantic context-key names are not interchangeable; invoice and transfer families still require the explicit `document` segment used by the cross-module contract | Keep the canonical document-key tuple in finance authoritative, make every inventory provider match it exactly, and build the combined plan before any live run | Never derive cross-module context names from operation IDs without an exact parity assertion against the consumer-owned tuple | The combined planner named all five missing providers, then passed after the provider keys matched the canonical document targets; all 68 focused and 240 read-case tests passed offline |
| 2026-07-21 | Offline implementation of real-repository read-registry parity | The plan referenced `LivePolicy.AUTOMATIC` and `LivePolicy.BLOCKED`, but no `LivePolicy` enum exists in the safety implementation | `OperationSafety.live_policy` is a validated string restricted by the catalog parser to the supported policy vocabulary | Compare committed catalog values to the exact strings `automatic` and `blocked`; do not invent an enum solely in tests | Verify plan pseudocode against current public dataclass field types before turning it into executable parity assertions | Source inspection confirmed the string field and parser allowlist; the corrected parity test uses the real catalog API |
| 2026-07-21 | Offline `verify-no-secrets` after adding synthetic live-safety fixtures and shifting older test lines | The gate returned only its fixed detector failure while no active credential was exposed | One new synthetic JWT lacked an inline allowlist, and three already audited false positives retained stale line numbers; invoking the hook directly would also rewrite the working baseline | Inspect only detector metadata, mark the reviewed synthetic literal with `pragma: allowlist secret`, and update only stale line numbers after a temporary-copy audit proves identical finding identities with zero additions/removals | Add the inline allowlist with each intentional synthetic secret and run all baseline diagnostics against a disposable copy, never the audited working file | The temporary audit reported unchanged finding identities and exactly three line moves; the ordinary project secret verification then passed |
| 2026-07-22 | Guarded `POST /api/1/organizations/settings` read with only `organizationIds` | The endpoint returned HTTP 500 even though the OpenAPI request parameters are optional | The live endpoint requires an explicit setting selector for this safe case despite the published optionality | Send the one-variable request with `parameters: ["AddressFormatType"]` | Keep the guarded case explicit and do not infer that omitted optional selectors are accepted live | The guarded request with the explicit public selector returned HTTP 200 |
| 2026-07-22 | Generated selected capture after a successful HTTP 200 read | Capture publication failed locally with `capture_failed` | The generated adapter supplied `duration_ms`, while `CaptureWriter` accepts capture metadata key `duration` in seconds | Map the integer milliseconds to seconds under `duration` at the adapter boundary | Retain an offline generated-read contract regression that uses the real writer and asserts both metadata vocabularies | The real-writer regression failed on `duration_ms`, then passed with exact `duration == 0.125` while the generated result retained integer milliseconds |
| 2026-07-22 | Three guarded representatives under `PublicApiInvoiceProcessing`: finance incoming services, inventory counteragents, and inventory incoming invoices | Each complete request returned HTTP 403; the finance list request already contained every public `ListRequest` field | The selected environment lacks the `PublicApiInvoiceProcessing` entitlement; this is profile-specific and not a malformed-payload or generic-403 condition | Declare `disabled_read_capabilities = ["public_api_invoice_processing"]` in that private profile so all 28 family reads become `no_live_target` before request construction or rate acquisition; do not retry or switch API login | Keep every real case and builder in the exhaustive plan, validate the exact typed capability in the strict profile schema, and remove the flag only after separately verified entitlement access | Focused offline profile, read-case, runner, finance, and inventory regressions passed without any live call |
| 2026-07-22 | Fresh GitHub Actions checkout ran the full Ruff scope after local cached lint had passed | Both Python matrix jobs stopped at lint with `I001` for a generated contract test, and every later offline step was skipped | The local Ruff cache retained an outdated import-classification result and hid a missing boundary between third-party `pytest` and first-party `iikocloud_client`; a fresh traversal classified the SDK from `src/` correctly | Organize the import block and run the canonical full lint with `ruff check --no-cache tools tests` | Keep the no-cache flag in the workflow contract so release verification cannot rely on stale local analysis | The raw Actions log identified the single file; the same Ruff version reproduced the exact `I001` with cache disabled, then the corrected cold lint and nine focused contract tests passed |
| 2026-07-22 | GitHub Actions ran the evidence-candidate writer suite on Python 3.10 | The body exception was preserved, but its sanitized cleanup-failure note was absent | `BaseException.add_note` is available only on Python 3.11 and newer, while the package supports Python 3.10; the compatibility branch silently discarded the note | When `add_note` is unavailable, append the same fixed type-only message to a plain `__notes__` string list and suppress any exceptional custom-object behavior | Keep a regression that disables `add_note` on a real exception and rejects cleanup payload text in the fallback note | The compatibility regression failed with no `__notes__`, then the fallback and all 139 writer tests passed offline |
| 2026-07-22 | GitHub Actions ran repository parity tests from a clean checkout | Two live-support tests raised `FileNotFoundError` for `build/openapi/effective.json` | The ignored effective artifact existed in the development checkout but is created only by a later CI verification step | Compose the effective schema in memory from the committed upstream snapshot, overlays, operation IDs, and model overrides | Repository parity tests must consume the read-only committed-input composer; never require an ignored build cache | A temporary-repository regression composed corrected schema with no effective artifact or write, and the two failed parity nodes then passed offline |
| 2026-07-22 | GitHub Actions collected the remaining offline suite on Python 3.10 | Collection stopped with `ModuleNotFoundError: tomllib`; the later pipeline version reader had the same unsupported direct import, and a package-lock assertion assumed the 3.12 marker result | `tomllib` entered the standard library in Python 3.11, two callers bypassed the declared `tomli` fallback, and `exceptiongroup` is correctly active only below 3.11 | Select the version-compatible TOML module and make environment-marker expectations depend on the interpreter being tested | Audit every TOML import and version-conditioned locked dependency whenever Python support metadata changes; keep both supported versions in the offline CI matrix | The raw 3.10 log identified collection failure, exhaustive searches found every import and marker assertion, and focused package/pipeline tests plus cold Ruff and mypy passed after compatibility fixes |
| 2026-07-22 | GitHub Actions ran final pipeline verification with a fresh uv cache | The isolated wheel's exact locked runtime installation failed offline even though the same packages had been installed by `uv sync` | A lockfile URL can populate uv's artifact cache without populating the registry resolver metadata used by a later name-based `uv pip install --offline`; direct PEP 751 sync had the inverse cache mismatch | After frozen environment sync, explicitly run `prime-package-check-cache`; it refresh-compiles the exact reviewed pins without dependencies, checks the output against the lock closure, and leaves all wheel verification offline | Keep cache priming as an explicit sanitized online setup step and retain `--offline --no-deps` on both isolated wheel installs | A brand-new cache reproduced the failure, the compile prime populated the missing metadata, and both staged and root isolated wheel installs then completed offline during full verification |
| 2026-07-22 | Local fresh-cache CI reproduction omitted the workflow's global bytecode setting | Both offline wheel checks passed, but the last committed-tree gate reported a generated-manifest mismatch | Invoking the pipeline without `PYTHONDONTWRITEBYTECODE=1` created ignored `__pycache__` files inside the manifest-controlled committed SDK | Remove only the generated bytecode artifacts and rerun with `PYTHONDONTWRITEBYTECODE=1`, matching the workflow environment | Preserve the global workflow variable and use it for manual generation and verification commands | The rerun with the clean committed package and workflow-equivalent environment completed the full offline `verify` successfully |
| 2026-07-23 | Live calls to `POST /api/1/loyalty/iiko/get_counters` through the generated SDK | Every request with SDK enum values failed with HTTP 400 `Validation_CounterValidationError`; requests without `metrics`/`periods` failed with HTTP 500 | The upstream schema declares `CounterMetric`/`CounterPeriod` as integer enums (Swashbuckle-style reflection), but the endpoint binds them with a Newtonsoft string enum converter and then business-validates the accepted sets; integer values only deserialize and are then rejected, and the generated `int` enums cannot express the required string names | Guarded overlay `counter-enums-string-*` converts both schemas to string enums with the live-verified sets (`OrdersCount`, `OrdersSum`; `AllTime`, `Day`, `Week`, `Month`, `Quarter`, `Year`), followed by offline regeneration; accepted sets were enumerated from the endpoint's own validation messages via guarded probes that listed every invalid candidate element at once | Treat integer enum declarations in upstream as unverified until a contract test or live check proves the wire format; enumerate server-accepted values through the endpoint's validation errors instead of guessing CLR member names | `tests/contracts/test_counter_enums.py` failed before the overlay and passes after `sync --offline`; the updated `get_loyalty_counters` read case sends the verified string values |
| 2026-07-23 | Selected live read of `list_finance_incoming_services` with `--capture-http` | The operation returned HTTP 200 but the run aborted with `capture_failed` | `LiveCapture._json_value` converted Pydantic models only at the top level; a top-level response array left Pydantic items inside the list, and the strict JSON writer failed closed | Recursively convert Pydantic models inside lists and dicts before strict validation; covered by `test_live_capture_dumps_top_level_list_of_pydantic_models` | Treat top-level array responses as a distinct capture shape and replay every newly capturable response shape offline before the live run | The new regression failed before the fix and the full capture suite (118 tests) passes after it |
| 2026-07-23 | Selected live read of `get_inventory_counteragents` under the v2 authentication contract | HTTP 400 with `EXTERNAL_SYSTEM_TIMEOUT` after the same endpoint returned HTTP 403 under the v1 contract | The v2 application token clears the entitlement gate, but the backend invoice-processing system times out for this environment; the endpoint is unusable under either contract | Keep the read case skipped as `no_live_target/endpoint_unavailable` before request construction; revisit only after a separately verified backend fix | Do not interpret a changed error class as endpoint recovery; verify the response body class before re-enabling a skipped read | Guarded probe returned the sanitized timeout error body; the case remains skipped with the evidence recorded in its builder comment |
| 2026-07-24 | First guarded `live_write` run of `test_stop_list.py` | The test crashed with `AttributeError: GeneratedLiveSdk object has no attribute call_generated` before any HTTP call | The write test predated the adapter hardening that closed arbitrary callback substitution; no sanctioned path existed for the compensating `add_products_to_stop_list` operation | Added `execute_compensating` with strict profile-boundary validation mirroring `execute_cleanup`, rewrote the test to `call_bound_read` preflight + `execute_compensating` + journal cleanup, and added offline adapter regressions | Any live test that never ran must be assumed broken against the current adapter; run every guarded path at least once before relying on it | Offline adapter tests passed (55), and the live write round-trip completed with a clean journal and a completed receipt |
| 2026-07-24 | `get_terminal_groups` on the write stand returned an empty list while the group existed in iikoWeb | Read-case selected run failed `assertion_failed`; discovery without `includeDisabled` reported zero groups | Terminal groups not granted to the API login under Cloud API organization settings are hidden from the default response and appear only with `includeDisabled: true`; the write stand's group is in that state (and the Cloud API UI can block granting with an outdated-plugin message), while stop-list writes on the hidden group are still accepted | Discovery now always requests `includeDisabled: true` and marks sleeping groups; treat "hidden from reads" as an API-access condition, not an RMS-disabled one; write verification must not rely on read visibility | Record API-access state separately from RMS state before diagnosing "missing" terminal groups | Discovery regression updated; live discovery reported the group, and the write round-trip succeeded against the hidden group |
| 2026-07-24 | First live run of the `customer` write lifecycle on the write stand | The prepare read `get_customer_info` failed with HTTP 400 `Common_OrganizationNotFound` (numeric backend organization id in the message) before any mutation | The loyalty backend (iikoNet) is not provisioned for the write stand organization, so every loyalty endpoint rejects the organization lookup | Keep the `customer` scenario `enabled: false` in `contracts/write-lifecycles.yaml` with the recorded reason until loyalty is provisioned in iikoWeb; the collection gate rejects the scenario before any live call | A lifecycle scenario must be verified against the target environment before enabling; an environment capability failure disables the scenario, never weakens the gates | Guarded probe returned the sanitized 400 body; registry and gate tests updated and passing |
| 2026-07-24 | Customer write lifecycle: `create_or_update_customer` with a client-generated UUID as `id` | HTTP 400 with a "login entry not found" style message; the compensation (`delete_customers` on the never-created id) also failed and the mutation journal stayed pending | For this endpoint a supplied `id` means "update the existing customer"; iiko assigns ids itself on create, and delete of a non-existent id is rejected | Create without `id`, read the assigned id from the create response, register the compensation with that id immediately after create; the window between create and journal registration is process-local and documented | Never pre-generate entity ids for iiko create endpoints; design lifecycle cleanup around server-assigned ids | After the rework the full chain (create → verify by id → delete) passed live with a completed receipt and no journal residue |
| 2026-07-24 | Expected "not found" probe inside a guarded live test (`get_customer_info` by phone) | The 400 response (`Validation_IncorrectPhone`) terminated the guarded session before the scenario could continue | The guarded adapter treats any non-2xx as terminal by design (no-retry discipline); iiko signals "customer not found" with HTTP 400, not an empty 200 | Do not probe absence inside a lifecycle run; rely on ownership markers, server-assigned ids, and the pending-journal gate to surface leftovers | Treat expected-4xx flows as incompatible with the guarded session; verify absence through post-run reports or a dedicated probe run | Documented in the customer lifecycle test; the reworked scenario needs no absence probe |
| 2026-07-24 | Mutation journal left pending after a failed write scenario; `cleanup-orphans` could not execute the compensation | Orphan cleanup retried `delete_customers` on a customer that was never created and failed again, keeping the journal pending forever | The compensation target did not exist (verified by probe), so the action was moot; the contour also refuses to auto-delete completed journal residue ("requires manual review") | Manual review path: verify mootness with a guarded probe, mark the entry done, rewrite the journal with the repo canonical encoder, review the completed residue, then remove it; record the review in this ledger | Keep compensations tied to server-confirmed entities so moot entries cannot occur | Journal resolved and removed; the subsequent full lifecycle run completed with a clean mutations directory |
